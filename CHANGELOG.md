# 更新记录

## 0.4.1 - 多站点首次数据与配置自愈

- 安装和升级阶段预热尚无统计的站点，完成安装后再启动实时服务，减少首次打开页面等待。
- 补录判断由全库状态改为逐站状态，并公平分配时间预算，避免首个大日志阻塞后续站点。
- 升级已有数据时仍会补录缺失统计的第二站点及后续站点。
- 实时服务启动时立即同步全部 Web 服务扩展配置，新增站点自动同步间隔由 60 秒缩短为 15 秒。
- 串行化安装脚本、常驻服务和面板修复的配置操作，避免并发修改 Nginx / Apache 配置。
- 补录状态按当前站点展示，不再因其他站点预热而误报当前站点仍在补录。

## 0.4.0 - 网站列表

- 进入阶段 B，新增全部宝塔站点的集中统计列表。
- 批量聚合各站点 PV、UV、IP、请求、流量、错误率和最后数据时间，避免逐站查询。
- 展示实时服务、站点接入和等待访问状态，并标识 Nginx / Apache。
- 支持网站搜索、指标排序、时间范围切换及从列表直接进入站点概览。

## 0.3.9 - 指标帮助提示修复

- 调整指标名称与问号图标的基线、尺寸和间距，修复图标位置不协调的问题。
- 用插件内置悬停提示替代浏览器原生 `title`，补齐 PV、UV、IP、流量、请求、实时流量和每秒请求的统计口径说明。
- 为键盘聚焦与辅助技术补充完整指标说明，并处理首尾卡片提示框越界。

## 0.3.8 - 仓库目录整理

- 页脚精简为 `Developer - Meiam`，不再展示其他文字。
- 将开发规划移入源码仓库，清理早期界面预览脚手架、依赖和历史发布包。

## 0.3.7 - 项目来源与开发基线

- 插件主页及安装来源标记为 `https://592.la`。
- 面板页脚开发者信息改为纯英文，并增加英文安装来源标记。
- 建立独立本地 Git 仓库，作为后续功能开发与版本追踪基线。

## 0.3.6 - 字体与趋势图视觉优化

- 统一 10/11/12/13/14/15/24px 字号层级，正文与控件以 13–14px 为主，核心指标采用等宽数字排版。
- 指标区改为独立轻量卡片，补充悬停、选中和涨跌颜色反馈，提升七项指标的辨识度。
- 趋势图升级为平滑折线、柔和面积渐变、虚线网格、清晰坐标和带数值标题的数据点。
- 图例、指标切换、空状态、通知条及窄窗口响应式布局同步优化。
- 静态资源增加版本参数，避免宝塔浏览器缓存继续显示旧样式。

## 0.3.5 - 升级数据持久化

- 统计库、配置、游标与运行状态迁移到 `/www/server/webanalytics/data`，不再放在可能被宝塔覆盖的插件代码目录。
- 首次升级自动复制旧 `WebAnalytics/data` 内容，并使用迁移标记避免后续旧目录反向覆盖新数据。
- 卸载时仍先备份持久数据，再清理插件和独立数据目录。

## 0.3.4 - 后台补录与服务心跳

- 首次日志补录改为后台线程，Socket 接收和服务心跳不再被最长 45 秒的补录过程阻塞。
- 服务启动后立即写入心跳，界面区分“首次数据补录中”和真正的服务停止。
- 捕获并持久化服务致命错误，前端可直接显示原因，systemd journal 同时保留完整堆栈。

## 0.3.3 - 首次启动就绪检查修复

- 实时服务先创建 Unix Socket，再执行可能耗时数十秒的首次日志补录，避免安装脚本误报服务启动失败。
- 安装健康检查等待窗口由 5 秒延长至 15 秒，兼容负载较高的宝塔服务器。

## 0.3.2 - 宝塔原生站点发现与首次数据补录

- 与官方 `site_total` 保持一致，优先使用宝塔 `public.M('sites')` 获取真实面板站点，直接读取 SQLite 仅作为降级方案。
- 数据库尚无统计时，采集服务会从每个站点现有访问日志尾部最多 32 MB 补录，安装后无需等待新请求即可看到近期数据。
- 尾部补录会跳过截断产生的第一条残行，不影响正常追加读取和日志轮转游标。
- 继续使用常驻服务采集；不创建负责采集的计划任务。

## 0.3.1 - 宝塔常驻进程兼容修复

- 面板接口使用版本化独立运行命名空间，彻底避开旧版通用 `core` 模块缓存与其他插件同名模块冲突。
- 插件构造阶段不再把初始化异常直接抛给宝塔，接口会返回具体错误而不是无信息的 HTTP 500。
- 升级数据库前先停止旧采集服务，避免 HLL 迁移期间发生 SQLite 锁竞争。
- 自动补齐早期测试版数据库缺少的站点、游标及指标字段，保留已有统计数据。

## 0.3.0 - 官方精简版架构对齐

- 改用宝塔站点 `extension/<站点>/webanalytics.conf` 接入，升级时自动移除 0.2.x 直接注入块。
- 同时支持 Nginx 与 Apache，并每 60 秒同步新增、删除和切换 Web 服务的站点。
- 扩展结构化日志字段，按可信代理网段解析 CDN/反向代理真实客户端 IP。
- 新增有界写入队列、丢弃数、写入异常与配置同步诊断。
- UV/IP 改用按小时 HyperLogLog 近似去重；旧精确去重数据自动迁移且保留回滚表。
- 扩充常见搜索引擎、SEO 和 AI 爬虫识别，PV 排除爬虫、静态资源和错误响应。
- 所有数据继续仅保存在本机，不包含官方二进制中的云端提交逻辑。
- 兼容宝塔面板升级后短暂保留旧 Python 模块缓存的情况，避免 `retain_sites` 接口缺失。

## 0.2.2 - 布局与采集诊断

- 按浏览器可视区域将插件弹窗水平、垂直居中。
- 网站列表仅来自宝塔站点数据库，默认不再扫描孤立日志文件。
- 区分服务存活、Socket 就绪、Nginx 已接入和当前站点已收到请求。
- 零数据时显示具体原因，并提供可回滚的采集配置修复操作。

## 0.2.1 - 界面修复

- 修复加载完成后占位区域仍显示并撑高页面的问题。
- 修复工具栏和上方导航被滚出可视区域的问题。
- 修复零数据趋势图纵轴重复显示“1”的问题。

## 0.2.0 - 实时采集架构

- 移除一分钟 cron 采集。
- 新增独立 Unix Socket `/tmp/webanalytics.sock` 与 systemd 守护服务。
- Nginx 通过 syslog 实时发送结构化访问日志，目标延迟小于 3 秒。
- 站点配置修改前备份，执行 `nginx -t` 成功后才重载，失败自动回滚。
- 卸载时先清理 Nginx 转发配置，再停止服务，避免残留无效 socket 引用。
- 保留文件增量读取作为历史回溯与故障恢复能力。

## 0.1.0 - 阶段 A

- 建立宝塔 Linux 面板 v11.x 插件骨架与安装生命周期。
- 增量读取 Nginx combined 访问日志，并保存 inode/offset 游标。
- 支持日志截断和常见重命名轮转续读。
- 使用 SQLite WAL 聚合分钟请求、PV、流量、错误和蜘蛛请求。
- 按日哈希去重 UV 与独立 IP。
- 新增面板内网站选择、时间筛选、指标卡和趋势图。
- 新增采集健康状态与手动立即采集。
- 默认离线运行，不修改 Web 服务配置，不回溯安装前历史日志。
